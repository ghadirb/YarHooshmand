// Import لازم برای RepositoriesMode
import org.gradle.api.initialization.resolve.RepositoriesMode

pluginManagement {
    repositories {
        gradlePluginPortal()
        google()
        mavenCentral()
    }
}

dependencyResolutionManagement {
    // استفاده صحیح از RepositoriesMode
    repositoriesMode.set(RepositoriesMode.ALLOW)
    repositories {
        google()
        mavenCentral()
    }
}

rootProject.name = "YarHooshmand"
include(":app")
